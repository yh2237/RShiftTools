<Window
    x:Class="RShiftTools.Views.CompressDialog"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:converters="clr-namespace:RShiftTools.Converters"
    Title="ファイルサイズ縮小 - RShiftTools"
    Width="560"
    Height="440"
    ResizeMode="CanMinimize"
    WindowStartupLocation="CenterScreen">

    <Window.Resources>
        <converters:StatusConverter x:Key="StatusConverter" />
        <BooleanToVisibilityConverter x:Key="BoolToVis" />
        <Style x:Key="StatusTextStyle" TargetType="TextBlock">
            <Style.Triggers>
                <DataTrigger Binding="{Binding Status}" Value="Done">
                    <Setter Property="Foreground" Value="#2ecc71" />
                </DataTrigger>
                <DataTrigger Binding="{Binding Status}" Value="Error">
                    <Setter Property="Foreground" Value="#e74c3c" />
                </DataTrigger>
                <DataTrigger Binding="{Binding Status}" Value="Processing">
                    <Setter Property="Foreground" Value="#3498db" />
                </DataTrigger>
                <DataTrigger Binding="{Binding Status}" Value="Cancelled">
                    <Setter Property="Foreground" Value="#f39c12" />
                </DataTrigger>
            </Style.Triggers>
        </Style>
    </Window.Resources>

    <Grid Margin="16">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto" />
            <RowDefinition Height="*" />
            <RowDefinition Height="Auto" />
            <RowDefinition Height="Auto" />
        </Grid.RowDefinitions>

        <Border
            Grid.Row="0"
            Margin="0,0,0,8"
            Padding="12,8"
            BorderBrush="#DDD"
            BorderThickness="1"
            Visibility="{Binding IsVideoMode, Converter={StaticResource BoolToVis}}">
            <Grid>
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="Auto" />
                    <ColumnDefinition Width="100" />
                    <ColumnDefinition Width="Auto" />
                    <ColumnDefinition Width="Auto" />
                    <ColumnDefinition Width="*" />
                </Grid.ColumnDefinitions>
                <Grid.RowDefinitions>
                    <RowDefinition Height="Auto" />
                    <RowDefinition Height="Auto" />
                    <RowDefinition Height="Auto" />
                    <RowDefinition Height="Auto" />
                </Grid.RowDefinitions>

                <TextBlock
                    Grid.Row="0"
                    Grid.Column="0"
                    Margin="0,0,8,6"
                    VerticalAlignment="Center"
                    Text="目標サイズ：" />
                <TextBox
                    Grid.Row="0"
                    Grid.Column="1"
                    Margin="0,0,8,6"
                    Text="{Binding TargetSizeMb, UpdateSourceTrigger=PropertyChanged}" />
                <TextBlock
                    Grid.Row="0"
                    Grid.Column="2"
                    Margin="0,0,0,6"
                    VerticalAlignment="Center"
                    Text="MB" />

                <TextBlock
                    Grid.Row="1"
                    Grid.Column="0"
                    Margin="0,0,8,6"
                    VerticalAlignment="Center"
                    Text="音声品質：" />
                <ComboBox
                    Grid.Row="1"
                    Grid.Column="1"
                    Grid.ColumnSpan="3"
                    Margin="0,0,0,6"
                    ItemsSource="{Binding AudioQualities}"
                    SelectedItem="{Binding AudioQuality}" />
                <TextBlock
                    Grid.Row="2"
                    Grid.Column="0"
                    Margin="0,0,8,0"
                    VerticalAlignment="Center"
                    Text="ハードウェア：" />
                <ComboBox
                    Grid.Row="2"
                    Grid.Column="1"
                    Grid.ColumnSpan="3"
                    ItemsSource="{Binding HwEncoders}"
                    SelectedItem="{Binding HwEncoder}" />
                <TextBlock
                    Grid.Row="3"
                    Grid.Column="0"
                    Grid.ColumnSpan="5"
                    Margin="0,6,0,0"
                    Foreground="#555"
                    Text="{Binding EstimateText}" />
            </Grid>
        </Border>

        <Border
            Grid.Row="0"
            Margin="0,0,0,8"
            Padding="12,8"
            BorderBrush="#DDD"
            BorderThickness="1"
            Visibility="{Binding IsImageMode, Converter={StaticResource BoolToVis}}">
            <StackPanel>
                <TextBlock
                    Margin="0,0,0,6"
                    Foreground="#555"
                    Text="{Binding ImageQualityLabel}" />
                <Slider
                    IsSnapToTickEnabled="True"
                    Maximum="{Binding ImageQualityMax}"
                    Minimum="{Binding ImageQualityMin}"
                    TickFrequency="1"
                    Value="{Binding ImageQuality}" />
            </StackPanel>
        </Border>

        <ListView
            Grid.Row="1"
            Margin="0,0,0,8"
            ItemsSource="{Binding Files}">
            <ListView.View>
                <GridView>
                    <GridViewColumn
                        Width="240"
                        DisplayMemberBinding="{Binding FileName}"
                        Header="ファイル名" />
                    <GridViewColumn
                        Width="80"
                        DisplayMemberBinding="{Binding FileSizeText}"
                        Header="サイズ" />
                    <GridViewColumn Width="80" Header="進捗">
                        <GridViewColumn.CellTemplate>
                            <DataTemplate>
                                <ProgressBar
                                    Width="70"
                                    Height="14"
                                    Maximum="100"
                                    Value="{Binding Progress}" />
                            </DataTemplate>
                        </GridViewColumn.CellTemplate>
                    </GridViewColumn>
                    <GridViewColumn Width="80" Header="状態">
                        <GridViewColumn.CellTemplate>
                            <DataTemplate>
                                <TextBlock
                                    Style="{StaticResource StatusTextStyle}"
                                    Text="{Binding Status, Converter={StaticResource StatusConverter}}"
                                    ToolTip="{Binding ErrorMessage}" />
                            </DataTemplate>
                        </GridViewColumn.CellTemplate>
                    </GridViewColumn>
                </GridView>
            </ListView.View>
        </ListView>

        <StackPanel Grid.Row="2" Margin="0,0,0,8">
            <ProgressBar
                Height="18"
                Margin="0,0,0,4"
                Maximum="100"
                Value="{Binding TotalProgress}" />
            <TextBlock Foreground="#555" Text="{Binding StatusText}" />
        </StackPanel>

        <Grid Grid.Row="3">
            <Button
                Width="130"
                HorizontalAlignment="Left"
                Click="OpenFolderButton_Click"
                Content="フォルダを開く"
                IsEnabled="{Binding HasOutput}" />
            <StackPanel HorizontalAlignment="Right" Orientation="Horizontal">
                <Button
                    Width="90"
                    Margin="0,0,8,0"
                    Click="CancelButton_Click"
                    Content="キャンセル" />
                <Button
                    Width="90"
                    Click="RunButton_Click"
                    Content="圧縮開始"
                    IsEnabled="{Binding CanRun}" />
            </StackPanel>
        </Grid>
    </Grid>
</Window>
